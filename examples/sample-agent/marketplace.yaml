apiVersion: ai.anz.com/v1
kind: AgentCapability
metadata:
  name: payments-agent
  version: 1.0.0
  owner: payments-platform
  supportChannel: "#payments-ai-support"
spec:
  type: skill
  riskLevel: medium
  dataClassification: confidential
  runtime:
    authRequired: true
  capabilities:
    tools: []
  security:
    requiresAuditLogging: true
  eval:
    casesFile: eval-cases.yaml
    minimumScore: 85
